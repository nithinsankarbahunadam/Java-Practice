public class SumArray {
    public static void main(String[] args) {

        int[] numbers = {10, 20, 30, 40, 50};

        int sumValue = 0;

        for (int i = 0; i < numbers.length; i++) {
            sumValue += numbers[i]; //sumValue = sumValue + numbers[i];
        }
        
    }
    
}
